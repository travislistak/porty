<footer class="navbar navbar-footer py-3 my-4 border-top">
  <div class="container d-flex flex-column justify-content-center">
    <ul class="navbar-nav flex-row border-bottom mb-1">
      <li class="nav-item me-3">
        <a class="nav-link" aria-current="page" href=<%= root_path %>>Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" aria-current="page" href=<%= root_path %>>Hire Me</a>
      </li>
    </ul>
    <div class="d-flex align-items-center justify-content-center text-center">
      <% if user_signed_in? %>
        <span class="mb-3 mb-md-0 text-muted">Travis Listak <%= link_to "©", destroy_user_session_path, method: :delete, class: "stealth-link", data: { turbo: 'false' } %>
          2024</span>
      <% else %>
        <span class="mb-3 mb-md-0 text-muted">Travis Listak <%= link_to "©", new_user_session_path, class: "stealth-link", data: { turbo: 'false' } %>
          2024</span>
      <% end %>
    </div>
  </div>
</footer>