version: "1.0"
services:
  web:
    image: hythagythgta/porty
    build:
      context: .
      args:
        RAILS_ENV: ${RAILS_ENV}
        ENV_MASTER_KEY: ${ENV_MASTER_KEY}
    command: rails server -b 0.0.0.0
    ports:
      - "80:3000"
